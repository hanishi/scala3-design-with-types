# Step 0: 型のない世界

型情報が失われると何が起きるか、体感してみよう。

## 0-1. 何でも入るリスト

```scala
{{#include ../../../examples/step0/step0.scala}}
```

**試してみよう：**
```
> scala-cli run step0.scala
```

`first * 2` のコメントを外してコンパイルしてほしい。エラーを読もう。
コンパイラは `Any` に `*` メソッドがないと言う。

**ポイント：** 実行時には値は 100 *だ*が、コンパイラはそれを知らない。
型情報が失われた瞬間、コンパイラはもう守ってくれない。
